//JavaScript script that updates the text color of the header where when it's clicked it turns red
// using the jQuery API
$('#red_header').click(function () {
  $('header').css('color', '#FF0000');
});
